{
  "name": "pause-plate-manager-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Application mobile privée Pause & Plate Manager pour iOS et Android",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "sync": "cap sync",
    "prepare:native": "cap sync",
    "open:android": "cap sync android && cap open android",
    "open:ios": "cap sync ios && cap open ios",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/ios": "8.5.0",
    "@capgo/capacitor-printer": "8.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
